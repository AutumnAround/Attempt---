/* Функція isEven приймає число number.
Перевір чи є воно парним (ділиться на 2 без остачі). Для парних чисел повертаємо true, для непарних false. Дробові числа не є парними.
*/

isEven(number) {
    if number === %2(true);
    else(false)
} 

function isEven(number) {
    if (number % 2 === 0) {
        return true;
    } else {
        return false;
    }
}
